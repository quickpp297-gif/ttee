services:
  - type: web
    name: sms-gateway
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: sms-data
      mountPath: /var/data
      sizeGB: 1
